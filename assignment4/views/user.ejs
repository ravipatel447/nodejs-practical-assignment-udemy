<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>users</title>
  </head>
  <body>
    <% if(users.length===0) { %>
    <h1>no users</h1>
    <% } else{ %>
    <h1>hear are all users</h1>
    <ul>
      <% users.map((user)=>{ %>
      <li><%= user.username %></li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
